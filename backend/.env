PORT=4000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ticketrush
DB_USER=ticketrush
DB_PASSWORD=ticketrush123

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# Seat hold duration (minutes)
SEAT_HOLD_MINUTES=10
# Email provider
# Resend is used when RESEND_API_KEY is present.
# For real recipients, use a RESEND_FROM address on a verified Resend domain.
RESEND_API_KEY=re_CQnzwxEH_PyVkpYGnHEYeDkgx9xWUDPtn
RESEND_FROM="TicketRush <noreply@ticketrush.id.vn>"


# Optional SMTP fallback when Resend is not configured
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="TicketRush <noreply@ticketrush.id.vn>"
